[package]
name = "obscura_lib"
type = "lib"
authors = ["Obscura Protocol"]
compiler_version = ">=0.31.0"

# Obscura Shared Library
# Exports: Note struct, commitment/nullifier/derive_pk, Poseidon2 hash
# primitives, Merkle verification, and protocol constants.
#
# Used by: obscura-transfer (2-in/2-out), obscura-merge (N-in/1-out),
#          obscura-split (1-in/N-out), obscura-mint (future).

[dependencies]
